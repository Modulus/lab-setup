[lab_login]
{% for host in groups["lab_machine"] %}
{{host}}  ansible_user={{user_list[loop.index].name}} ansible_ssh_pass={{user_list[loop.index].password}} workshop_dir=/home/{{user_list[loop.index].name}}/workshop
{% endfor %}
